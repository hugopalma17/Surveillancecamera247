package com.hpalma.Surveillance247

// StreamingServer.kt temporarily disabled
// This file was causing compilation errors due to missing NanoHTTPD dependency
// Will be re-implemented once basic functionality is working

// Placeholder class to avoid compilation errors
class StreamingServer(port: Int) {
    private val TAG = "StreamingServer"

    fun updateFrame(frameData: ByteArray) {
        // Temporarily disabled
    }
}
